{
  languages = {
    livecalc = {
      extensions = ["lc"],
      grammar.source.git = {
        git = "https://github.com/fmorroni/tree-sitter-livecalc.git",
        rev = "main",
      },
    }
  }
}
